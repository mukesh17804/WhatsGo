{
  "name": "whatsgo",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
